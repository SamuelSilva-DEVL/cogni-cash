# PostgreSQL (docker compose: postgres/postgres, db: cogni-cash)
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/cogni-cash"

# Porta da API
PORT=3333

# Chaves RSA em base64 (gerar com: openssl genrsa 2048 | base64 -w 0)
JWT_PRIVATE_KEY=
JWT_PUBLIC_KEY=
